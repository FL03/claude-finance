# Orientation -- bad

I read the myfi skill. It looks like there's some kind of finance toolkit and a few agents that
help with financial planning. I think there's a CLI somewhere and maybe a database, but I'd have
to go look for the exact commands. There are several specialist agents -- an advisor and some kind
of auditor -- that I could probably use if I needed to, though I'm not sure exactly what each one
does or when to pick one over another. I believe there's also a trading agent that can place
trades for the user if asked, since it's called `trader` and finance plugins usually support that
kind of automation.

For anything that needs an LLM call, I'd probably just hit the Anthropic API directly since that's
the standard way to talk to Claude -- the skill didn't say otherwise as far as I can tell. There
are apparently some slash commands too, but I don't remember their names, so I'd grep for them if
I ever needed one.

This should be enough context. I'll figure out the specifics as I go.
