---
title: v0.0.0 close report - myfi "The Foundation"
date: 2026-07-13
author: coder (W7-integration-close) @ shepherd:coder
seed: .shepherd/docs/plans/v000.seed.md
plan: .shepherd/docs/plans/v000.plan.md
verdict: DONE_WITH_CONCERNS
---

# v0.0.0 close report - myfi "The Foundation"

## Verdict

**DONE_WITH_CONCERNS.** All 16 units' [ACCEPTANCE] predicates pass on a real re-run against the
committed tree (evidence per unit below). Two concerns keep this from a clean DONE: (1) Wave 7
(`W7-docs-readme`, `W7-integration-close`) is finished and green but not yet committed by root, so
it sits as working-tree state at the time of this report; (2) the release-gate `bin/myfi-test` is
not yet wired into `.claude/shepherd.toml [gates].check`, so root's between-wave gate still covers
only the toolkit lane. Neither concern blocks the code from being correct; both are named
follow-ups below with an owner and next action.

This report was written by the `W7-integration-close` coder during its audit-redo, not deferred
to a later "root finalize" pass: the first version of this file was a content-free stub that
failed audit for exactly that deferral. What follows is the real verdict.

## Evidence base

Every claim below points at one of: a commit SHA (`git show <sha> --stat`), a re-runnable test
command, or a file that exists in the tree right now. Full lane re-run at report time:

```
$ bash bin/myfi-test
toolkit pytest (services/toolkit):        30 passed                     -> PASS
services unittest (llm 14 + eval 45):     59 passed                     -> PASS
hooks (hooks/tests/run.sh):               26/26 passed                  -> PASS
integration (tests/integration/run.sh):   4/4 integration suites passed -> PASS
4/4 suites passed / PASS bin/myfi-test
```

`rg -n 'api\.(anthropic|openai)\.com' services/ commands/ agents/ skills/` returns 0 matches (LLM
law holds: every model call routes through `services/llm/llm.py` shelling to local Claude Code).

## Per-unit verdicts

| # | Unit | Wave | Commit(s) | Verdict | Evidence |
| :-- | :--- | :-: | :--- | :--- | :--- |
| 1 | W1-relocate | 1 | `dd4409a` | DONE | `tests/integration/test_plugin_load.py::RelocationInvariantTests::test_no_src_directory` passes; `.mcp.json` parses and registers `myfi-toolkit` at `${CLAUDE_PLUGIN_ROOT}/bin/myfi-mcp` (`McpManifestTests`). |
| 2 | W2-toolkit-scaffold | 2 | `8a46385` | DONE | `poetry -C services/toolkit run pytest -q` reports 30 passed. `bin/myfi-toolkit --version` / `bin/myfi-mcp` present and lazy-import heavy libs (no numpy/pandas/scipy/matplotlib/mcp at fast-gate import time, per plan `<2s` rule). |
| 4 | W2-services-llm-eval | 2 | `8a46385` | DONE | `python3 -m unittest discover -s services/llm/tests` reports 14 passed; `services/eval/tests` contributes to the 45-test eval count below. Mock seam (`MYFI_LLM_MOCK`) verified live in `test_analyze_e2e.sh` and `test_eval_rubrics.py::GoldenMarginTests`. |
| 6 | W2-myfi-skill | 2 | `8a46385` | DONE | `skills/myfi/SKILL.md` present, valid frontmatter (`test_plugin_load.py::SkillsLoadTests`); plan deviation log records root adding `tests/structure/test_skill_myfi.sh` + `services/eval/tests/test_skill_myfi_margin.py` outside the unit's own file scope (auditor blocker, root-fixed, logged in plan `Mid-sprint plan deviations`). |
| 3 | W3-toolkit-db | 3 | `5e99ef9`, `2d93d3f` | DONE | `test_toolkit_smoke.sh`: `db init` creates `.myfi/myfi.db`, `schema_versions >= 1`, idempotent re-run, all green at report time. Baseline migration checksum corrected to a real sha256 (auditor HIGH-confidence finding, root-fixed same wave, logged in plan deviations). |
| 5 | W3-toolkit-marketdata | 3 | `5e99ef9`, `2d93d3f` | DONE | `services/toolkit/tests/test_marketdata.py` (10 tests, part of the 30 toolkit-pytest total): typed `Quote`, `source == "research"` default, no network I/O on default path, unimplemented providers raise `NotImplementedError`. `test_toolkit_smoke.sh` confirms `quote AAPL` end-to-end via the real CLI. |
| 7 | W4-agents-actors | 4 | `239b619` | DONE | `agents/advisor.md`, `agents/quant.md` present, valid frontmatter (`AgentsLoadTests`, 6/6 agents at/above the required 6). |
| 8 | W4-agents-adversary-editor | 4 | `239b619` | DONE | `agents/auditor.md`, `agents/designer.md` present, valid frontmatter; `6553d0e` (cross-wave fix) registered `quote`/`db_init`/`db_migrate`/`db_version` as real MCP tools after an auditor HIGH-confidence finding that `@advisor` (no Bash tool) had no working data path, fixed same sprint, logged in plan deviations. |
| 9 | W4-agents-worker-trader | 4 | `239b619` | DONE | `agents/worker.md`, `agents/trader.md` present, valid frontmatter; `@trader` scaffold-only, zero live-execution surface (`trader_scaffold.rubric.json` + `trade_gate.rubric.json` shipped and structurally valid per `test_eval_rubrics.py`). |
| 10 | W4-skills-trio | 4 | `239b619` | DONE | `skills/improve/SKILL.md`, `skills/compliance/SKILL.md`, `skills/taxes/SKILL.md` present, valid frontmatter (`SkillsLoadTests`, 4/4 skills at/above required 4); plan deviation log records `compliance_coverage.rubric.json` + `taxes_knowledge.rubric.json` + goldens added after a critic concern that TAXES/COMPLIANCE lacked dedicated same-commit evals. |
| 11 | W5-commands-analyze-plan | 5 | `bb391e3` | DONE | `commands/analyze.md`, `commands/plan.md` present, valid frontmatter (`CommandsLoadTests`); `test_analyze_e2e.sh` mock-seam `/myfi:analyze` e2e green: last line `[ANALYZE] report artifact: ...`, artifact exists on disk, grounded in a real toolkit `quote()` call, carries the mocked agent-pass content. |
| 12 | W5-commands-taxes-trade | 5 | `bb391e3` | DONE | `commands/taxes.md`, `commands/trade.md` present, valid frontmatter; `commands/evals/taxes_{good,bad}.md` added per plan deviation log (critic concern: `taxes_workflow` rubric had no golden path in [FILES]). |
| 13 | W6-harness-loops | 6 | `86043a6` | DONE | Loop/workflow/agent-team templates ship under `hooks/` and pass `hooks/tests/run.sh` (part of the 26/26 total). |
| 14 | W6-harness-hooks | 6 | `86043a6` | DONE | `hooks/tests/run.sh` reports 26/26 passed at report time: `dispatch_guard.sh` (closed-flock enforcement, 10/10), `dedup_write_guard.sh` (DEDUP-HIT gate, 4/4), `session_venv.sh` (3/3), `adaptation_capture.sh` (IMPROVE harvest + roundtrip eval, 7/7), `hooks.json` wiring sanity + exec-bit checks (2/2). |
| 15 | W7-docs-readme | 7 | uncommitted (working tree) | DONE, pending wave-commit | Independently re-run at report time: `bash tests/structure/test_docs.sh` reports 7/7 ok, `PASS test_docs`, including `readme_finance eval margin: good=100 bad=20 threshold=65 margin=80`. `rg -n 'engineering work\|@engineer\|@coder\|sprint' README.md` returns 0 hits (the unit's own [ACCEPTANCE] grep, re-run directly, not assumed). `docs/{install,configuration,flock,toolkit,commands,marketdata}.md`, `docs/evals/{readme_good,readme_bad}.md`, and `examples/minimal/myfi.toml` all present. This coder does not own that unit's file scope and did not edit it: the verdict here is based on independently re-running its own stated acceptance predicate, not on trusting an unseen prior audit. |
| 16 | W7-integration-close | 7 | this redo (uncommitted, working tree) | DONE_WITH_CONCERNS | `bash tests/integration/run.sh` reports 4/4 integration suites passed (plugin-load, toolkit smoke, analyze e2e, close_report rubric wiring, the fourth suite added in this redo). `bash bin/myfi-test` reports 4/4 lanes passed. See "Concerns / follow-ups" for what this redo fixed and what remains open. |

## What this redo fixed (attempt 1 to attempt 2)

Audit attempt 1 failed with two blockers and one concern:

1. **Blocker: close report was a content-free stub.** Fixed: this file now carries the real
   per-unit verdict table above, cites concrete evidence for every claim, and names follow-ups
   explicitly (this section). `tests/integration/test_close_report_eval.py` now enforces this
   structurally (`test_shipped_close_report_is_not_the_bad_golden_shape`): a REDO back to a
   stub-shaped report is a loud gate failure, not a silent regression.
2. **Blocker: `close_report.rubric.json` had zero wiring anywhere in the repo.** Fixed:
   `tests/integration/test_close_report_eval.py` (new, in this unit's own file scope) mirrors the
   same self-contained golden-margin pattern `services/toolkit/tests/test_marketdata.py` uses for
   `market_quote.rubric.json` and `tests/structure/test_docs.sh` uses for
   `readme_finance.rubric.json`: `tests/integration/evals/close_report_{good,bad}.md` goldens,
   reproduced weighted-overall math (good=100, bad=20, threshold=65, margin=80, well above the
   15-point margin bar every sibling rubric holds), plus the structural tripwire named above.
   Wired into `tests/integration/run.sh` as a fourth suite and into `bin/myfi-test` transitively
   (it already ran the integration lane as a whole).
3. **Concern: `.claude/shepherd.toml [gates].check` only wires the toolkit pytest lane.**
   Correctly flagged (not silently changed) in `bin/myfi-test`'s own header comment, since that
   file is outside this unit's `[FILES]` scope. Not re-litigated here beyond naming it as a
   sprint-level follow-up (below): this coder attempted to file a tracking GH issue during this
   redo; the action was denied by the operator's tool-permission policy (external-write
   classifier), so the follow-up is named here instead, for a human or root-shepherd to act on.

## Concerns / follow-ups

- **`.claude/shepherd.toml [gates].check` covers only the toolkit lane.** Evidence: reading that
  file shows `check = "poetry -C services/toolkit run pytest -q"` only, while `bin/myfi-test`
  aggregates four lanes (toolkit pytest, services unittest, hooks, integration). Next action:
  extend `[gates].check` to call `bin/myfi-test` so root's between-wave / CI gate covers all four
  lanes. Owner: whoever next has `.claude/shepherd.toml` in scope (that file is outside every
  Wave 1-7 unit's `[FILES]`).
- **Wave 7 is not yet committed.** Both `W7-docs-readme` and `W7-integration-close` are green
  (re-verified above) but sit as uncommitted working-tree state at report time. Next action: root
  commits Wave 7 (`git add` the two units' disjoint file sets, one wave-boundary commit,
  mirroring the `feat(v0.0.0/wave-N): ...` message pattern used for Waves 1-6) once this report
  itself is reviewed.
- **No live-judge score exists for this or any close report yet.** `close_report.rubric.json`'s
  gate-lane wiring (above) proves the harness discriminates a good/bad close report by a clear
  margin under staged mock scores; it does not run the live local-Claude-Code judge against this
  actual file. Next action (optional, not gate-blocking): run
  `MYFI_EVAL_LIVE=1 python3 services/eval/eval.py run --kind=close_report --input-file=.shepherd/docs/reports/2026-07-13-v000-close.md --md`
  for a real judged score before treating this report as final.

## Scope note

This coder's file scope for this redo was exactly `tests/integration/**`, `bin/myfi-test`,
`.shepherd/docs/reports/2026-07-13-v000-close.md`, and `services/eval/rubrics/close_report.rubric.json`.
Every fix above stayed inside that scope; the `.claude/shepherd.toml` follow-up is named rather
than acted on because that file is out of scope for every W7 unit.
