---
title: Phase 0 drift check — myfi v0.0.0 (engineer, plan-time re-mesh)
date: 2026-07-13
author: engineer (opus) @ shepherd:engineer
mode: classic (root-run discovery consumed; verify-by-read only)
seed: .shepherd/docs/plans/v000.seed.md
planter_mesh: .shepherd/docs/reports/2026-07-13-planter-mesh.md
drift_verdict: NO SUBSTANTIVE DRIFT — 2 mechanical notes (both operator-decided post-seed), plan proceeds
---

# Phase 0 drift check — v0.0.0 "The Foundation"

Re-runs the seed §4 15-row mesh as a **drift check** against the planter mesh. Root-shepherd
already executed the `gh`/env probes (see GROUNDING); this pass **verifies by reading files only**
(no re-issued `gh`/network calls) and records drift since the planter mesh.

## 15-row mesh (drift check)

| # | Source | Seed pass condition | Plan-time result (verify-by-read) | Drift |
|---|--------|---------------------|-----------------------------------|-------|
| 1 | GH issues (FULL) | file deliverables as issues; classify | milestone #1 open, **0 issues**. Operator DEFERRED issue-filing post-seed: **`plan.md` is the deliverable ledger** (GROUNDING). | MECHANICAL — seed row-1 action superseded by operator decision; NOT an engineer rescope |
| 2 | GH PRs | none expected (greenfield) | draft release PR **#1** (`v0.0.0 → main`) exists (created by release pipeline). No source PRs. | MINOR — expected artifact of the patch-release pipeline; harmless |
| 3 | GH milestones | `v0.0.0` (#1) exists | present, open, 0 issues | none |
| 4 | git log | seed + config commits only | `62e2fe9` arm spawn brief, `099d1d0` update, `342f0e1` plant seed, `086732b` add .github, `cd7d679` initial — all config/seed, **no source** | none |
| 5 | Sentry | skipped (`[mcp].sentry` unset) | confirmed unset | none |
| 6 | Datastore (Supabase) | skipped (`[mcp].supabase` unset) | confirmed unset | none |
| 7 | Deploy (fly) | skipped (no `[cli].fly`) | confirmed absent | none |
| 8 | Prior close | none (first cycle) | confirmed — no `*-close.md` in reports/ | none |
| 9 | Prior handoff | none (first cycle) | confirmed — handoffs/ empty | none |
| 10 | Project CLAUDE.md | services-first + LLM-local + tests/evals binding | read end-to-end; binding doctrine intact | none |
| 11 | Carry-forward ledger | empty (first cycle) | confirmed empty | none |
| 12 | Knowledge silo | 3 discovery reports present | harvest + packaging + marketdata all present + read | none |
| 13 | Dedup-grep gate | wire-to-existing on hit | greenfield — no `myfi_toolkit`/`myctx` symbols exist yet; 0 hits. Enforce per-unit at write time. | none |
| 14 | Wrapper-grep gate | 0 hollow wrappers | N/A pre-code; enforced per-unit (bin wrappers are real dispatchers, not hollow) | none |
| 15 | Plugin auto-discovery | components at root, not `src/` | **`src/` still present** (`src/agents/`, `src/skills/`) — the Wave 1 relocation is the fix; `test ! -d src` is the gate | EXPECTED — this is the sprint's first deliverable, not drift |

## Environment drift (probe confirmation, read-only)

| Item | Planter/GROUNDING claim | Plan-time confirm | Note |
|------|-------------------------|-------------------|------|
| Python | 3.14.x / Poetry 2.x | Python **3.14.3**, Poetry **2.3.2** | favorable |
| numpy/pandas/scipy | importable in base python | numpy 2.4.4 / pandas 3.0.3 / scipy 1.17.1 import OK | favorable |
| matplotlib | NOT yet verified | **NOT importable** in base python (ImportError) → `poetry add`, **Agg backend, lazy import** | design constraint confirmed |
| mcp SDK | NOT installed | **NOT importable** → `poetry add mcp` | expected |
| `<2s` gate lane | stdlib-only / lazy heavy stack | binding: CLI `--version` + fast gates MUST NOT top-import numpy/pandas/matplotlib/mcp | carried into every toolkit unit's [NOTES] |

## src/ disposition (reconciliation resolved — see plan Wave 1 NOTES)

Seed skill set is `{myfi, improve, compliance, taxes}`; seed commands are `{analyze, plan, taxes, trade}`.
Current `src/skills/` is `{myfi, plan, advise, trade}` — a stale find/replace skeleton. Disposition:

- `src/skills/myfi` → **`skills/myfi`** (relocate, then rewritten as the MYFI core skill, Wave 2).
- `src/skills/plan` → **deleted**; the "plan" concept becomes the **`/myfi:plan` command** (Wave 5), not a skill.
- `src/skills/advise` → **deleted**; folds into the **`@advisor` agent** + **`/myfi:analyze` command**.
- `src/skills/trade` → **deleted**; becomes the **`/myfi:trade` command** (scaffold) + **`@trader` agent**.
- `src/agents/{advisor,auditor,quant,worker}` → **`agents/`** (relocate; fleshed out Wave 4). `@designer` + `@trader` are net-new agent files created in Wave 4.

## Open-issue ledger sweep

total open issues = **0**; drift-risk = **0**; NOT absorbed (operator decides) = **none**. First cycle,
empty carry-forward ledger, no chronic items. Nothing escalates.

## Drift verdict

**NO SUBSTANTIVE DRIFT.** Two MECHANICAL notes, both operator decisions taken *after* the seed was
written, neither a rescope by the engineer: (1) GH issue-filing is deferred — `plan.md` is the
deliverable ledger; (2) draft release PR #1 exists as a normal artifact of the patch-release
pipeline. The `src/` relocation (row 15) is the sprint's own Wave 1 work, not drift. Seed scope,
§5 locked decisions, and the W1→[W2∥W3]→W4→W5→W6 graph are all intact. **Plan proceeds.**
</content>
</invoke>
